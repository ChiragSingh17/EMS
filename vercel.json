{
  "buildCommand": "cd client && npm install && npm run build",
  "functions": {
    "server/api/index.js": {
      "maxDuration": 30
    }
  },
  "routes": [
    {
      "src": "/(.*)",
      "dest": "server/api/index.js"
    }
  ]
}
