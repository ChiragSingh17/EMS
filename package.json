{
  "name": "ems-fullstack",
  "private": true,
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "build": "npm run build --workspace client",
    "start": "node server/server.js"
  }
}
